import "dotenv/config";

function main() {
  throw new Error(
    "Disabled by strategy change: quality overwrite script is deprecated. Use non-destructive overlay/enrichment workflows."
  );
}

main();
